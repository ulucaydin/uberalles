(function () {
  'use strict';

  describe('Uberalles', function () {
    describe('Loading data from SFGOV', function () {
      it('after loading a truck existing area, there should be some trucks in the trucks dict', function () {
        //TODO
      });

      it('after loading a non SF area, there should be no new truck in the trucks dict', function () {
        //TODO
      });
    });

    describe('Changing the Radius Input', function () {
      it('After the change, new value should apply for both radius circle ', function () {
        //TODO
      });
      it('After the change, new value should apply for both', function () {
        //TODO
      });
    });

  });
})();
